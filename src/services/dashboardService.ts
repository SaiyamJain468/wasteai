import api from './api';

// Currently the backend doesn't have dashboard endpoints, so we'll simulate or use mock data
// But for consistency, let's create the service file.
// We can add endpoints later.

export const getDashboardStats = async () => {
  // const response = await api.get('/dashboard/stats');
  // return response.data;
  return null; // Placeholder
};
